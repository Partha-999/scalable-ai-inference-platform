apiVersion: v1
kind: Service
metadata:
  name: ai-inference-platform
spec:
  selector:
    app: ai-inference-platform
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
